transitData <- read.csv("MBTA_DATA.csv")
View(transitData)
View(transitData)
head(transitData)
summary(transitData)
View(transitData)
head(transitData)
filter(transitData, route_or_line == "Orange Line")
knitr::opts_chunk$set(echo = TRUE)
library(ggplot2)
library(tidyverse)
library(ggplot2)
library(tidyverse)
orangeLine <- transitData %>% filter(route_or_line == "Orange Line")
View(orangeLine)
transitData <- read.csv("MBTA_DATA.csv")
view(transitData)
transitData
head(transitData)
orangeLine <- transitData %>% filter(route_or_line == "Orange Line")
head(orangeLine)
orangeLine1 <- as.Date(orangeLine$ï..service_date, format = "%m/%d/%y")
head(orangeLine1)
orangeLine$ï..service_date <- as.Date(orangeLine$ï..service_date, format = "%m/%d/%y")
head(orangeLine)
orangeLine$ï..service_date <- as.Date(orangeLine$ï..service_date)
head(orangeLine)
?as.Date
orangeLine
head(orangeLine)
# filter the column "route_or_line" to only values of "Orange Line"
orangeLine <- transitData %>% filter(route_or_line == "Orange Line")
head(orangeLine)
orangeLine1 <- orangeLine %>% filter(substr(variable, 1, 1) == "1")
# orangeLine$ï..service_date
orangeLine1 <- orangeLine %>% filter(substr(ï..service_date, 1, 1) == "1")
head(orangeLine1)
View(orangeLine1)
View(orangeLine1)
# orangeLine$ï..service_date
orangeLine1 <- orangeLine %>% filter(substr(ï..service_date, 0, 1) == "1")
head(orangeLine1)
View(orangeLine)
# orangeLine$ï..service_date
orangeLine1 <- orangeLine %>% filter(substr(ï..service_date, 1,4) == "2016")
head(orangeLine1)
View(orangeLine1)
View(orangeLine)
# We can sort the data by the date (oldest to newest) so we can interpretate the data better
orangeLine <- orangeLine[order(orangeLine$ï..service_date),]
# We can sort the data by the date (oldest to newest) so we can interpretate the data better
orangeLine <- orangeLine[order(orangeLine$ï..service_date)]
# We can sort the data by the date (oldest to newest) so we can interpretate the data better
orangeLine2 <- orangeLine[order(orangeLine$ï..service_date),]
View(orangeLine)
# We can sort the data by the date (oldest to newest) so we can interpretate the data better
# orangeLine2 <- orangeLine[order(orangeLine$ï..service_date),]
orangeLine2 <- orangeLine[order(substr(orangeLine$ï..service_date,1,4)]
# We can sort the data by the date (oldest to newest) so we can interpretate the data better
# orangeLine2 <- orangeLine[order(orangeLine$ï..service_date),]
orangeLine2 <- orangeLine[order(substr(ï..service_date,1,4)]
# filter the column "route_or_line" to only values of "Orange Line"
orangeLine <- transitData %>% filter(route_or_line == "Orange Line")
head(orangeLine)
# We can sort the data by the date (oldest to newest) so we can interpretate the data better
# orangeLine2 <- orangeLine[order(orangeLine$ï..service_date),]
orangeLine2$Year <- substr(ï..service_date, 1,4)
# We can sort the data by the date (oldest to newest) so we can interpretate the data better
# orangeLine2 <- orangeLine[order(orangeLine$ï..service_date),]
orangeLine2$Year <- OrangeLine %>% substr(ï..service_date, 1,4)
# We can sort the data by the date (oldest to newest) so we can interpretate the data better
# orangeLine2 <- orangeLine[order(orangeLine$ï..service_date),]
orangeLine2 <- OrangeLine %>% substr(ï..service_date, 1,4)
# We can sort the data by the date (oldest to newest) so we can interpretate the data better
# orangeLine2 <- orangeLine[order(orangeLine$ï..service_date),]
OrangeLine %>% substr(ï..service_date, 1,4)
# We can sort the data by the date (oldest to newest) so we can interpretate the data better
# orangeLine2 <- orangeLine[order(orangeLine$ï..service_date),]
OrangeLine <- substring(ï..service_date, 1,4)
# We can sort the data by the date (oldest to newest) so we can interpretate the data better
# orangeLine2 <- orangeLine[order(orangeLine$ï..service_date),]
OrangeLine <- substr(ï..service_date, 1,4)
# We can sort the data by the date (oldest to newest) so we can interpretate the data better
# orangeLine2 <- orangeLine[order(orangeLine$ï..service_date),]
OrangeLine <- substr(ï..service_date, 1,4)
knitr::opts_chunk$set(echo = TRUE)
# We can sort the data by the date (oldest to newest) so we can interpretate the data better
# orangeLine2 <- orangeLine[order(orangeLine$ï..service_date),]
orangeLine4 <- orangeLine %>% arrange(ï..service_date)
library(ggplot2)
library(tidyverse)
# filter the column "route_or_line" to only values of "Orange Line"
orangeLine <- transitData %>% filter(route_or_line == "Orange Line")
head(orangeLine)
# We can sort the data by the date (oldest to newest) so we can interpretate the data better
# orangeLine2 <- orangeLine[order(orangeLine$ï..service_date),]
orangeLine4 <- orangeLine %>% arrange(ï..service_date)
# We can sort the data by the date (oldest to newest) so we can interpretate the data better
# orangeLine2 <- orangeLine[order(orangeLine$ï..service_date),]
orangeLine4 <- orangeLine %>% arrange(ï..service_date)
orangeLine4
